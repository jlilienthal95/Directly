export enum GenderPreference {
    Female = 'Female preferred',
    Male = 'Male preferred',
    NB = 'NB preferred'
  }
  
  export enum LightingRequirement {
    RingLight = 'Must have a Ring Light',
    Studio = 'Must have studio lighting',
    Daylight = 'Must use natural daylight',
    Night = 'Must record at night'
  }
  
  export enum PostType {
    Public = 'Public',
    Private = 'Private',
  }